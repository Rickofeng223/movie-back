import {joinDao as dao} from './../../src/db.js'

import  * as data from '../fixtures/db/db-data.js'
import {should} from "chai";

should()

export default () => {

    // it('moviesFromUserJoinReviews', async () => {
    //     const res = await dao.moviesFromUserJoinReviews(data.user)
    //     res.forEach(e => e.should.have.property('title'))
    //     res.should.be.eql(data.spiderman)
    //     console.log(res)
    // })
    // it('reviewJoinMovies', async () => {
    //     // dao.reviewJoinMovies()
    //
    // })
    // it('userJoinReviews', async () => {
    //     // dao.userJoinReviews()
    //
    // })
    // it('usersThatRatedMovie', async () => {
    //     // dao.usersThatRatedMovie()
    //
    // })
}